export const metadata = {
  title: "Smart Edge – How It Works",
  description: "Smart Edge tool documentation - how it works and why it matters",
};

export default function SmartEdgeInfoLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 